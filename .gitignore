# Generated at build/deploy time (see scripts/build-recent.py)
assets/data/json/recent_jobs.json

# Local dev / CI artifacts
server.log
_site/
node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Gerador de curriculo: fonte unica em project-classificavagas-page-resume.
# Traga com `python3 scripts/sync-resume.py` (o deploy faz isso automaticamente).
resume/
.resume-sha

# Catalogo de vagas: 47 MB por versao, publicado como release asset e baixado
# no build (scripts/fetch-catalog.py). Commitar isso levou o repositorio a
# 673 MB.
assets/data/json/open_jobs.json
assets/data/json/open_jobs.json.gz
assets/data/xlsx/
__pycache__/
